(function() {
  'use strict';

  angular
    .module('ticketmaster')
    .controller('AuthController', AuthController);

  /** @ngInject */
  function AuthController($timeout, webDevTec, toastr) {
    var vm = this;

    
  }
})();
